{
  "name": "kemp-registration",
  "private": true,
  "version": "1.0.0",
  "description": "Registration system for Kemp Baptist.",
  "type": "module",
  "scripts": {
    "dev": "http-server . -p 3000 --cors -o",
    "build": "cp -R src public index.html .env build/",
    "serve": "http-server build -p 3000 --cors"
  },
  "dependencies": {
    "dotenv": "^16.4.1",
    "mysql2": "^3.9.1"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
